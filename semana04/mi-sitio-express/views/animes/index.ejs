<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AnimeList - Mi Colección</title>
    <!-- Materialize CSS -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css">
    <!-- Material Icons -->
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <style>
        .anime-header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 40px 0;
        }
        .rating-badge {
            border-radius: 15px;
            padding: 5px 12px;
            color: white;
            font-weight: bold;
        }
        .rating-high { background-color: #4caf50; }
        .rating-medium { background-color: #ff9800; }
        .rating-low { background-color: #f44336; }
        .anime-card {
            height: 100%;
        }
        .anime-image {
            width: 100%;
            height: 300px;
            object-fit: cover;
            border-radius: 8px 8px 0 0;
        }
        .card-image-container {
            position: relative;
            overflow: hidden;
        }
        .card-overlay {
            position: absolute;
            top: 10px;
            right: 10px;
            z-index: 1;
        }
    </style>
</head>
<body>
    <div class="anime-header">
        <div class="container">
            <h1 class="center-align">
                <i class="material-icons large">movie</i>
                AnimeList
            </h1>
            <p class="center-align">Tu colección personal de animes</p>
        </div>
    </div>

    <div class="container" style="margin-top: 30px;">
        <div class="row">
            <div class="col s12">
                <div class="card-panel">
                    <div class="row valign-wrapper">
                        <div class="col s8">
                            <h4>Mi Colección (<%= animes.length %> animes)</h4>
                        </div>
                        <div class="col s4 right-align">
                            <a href="/animes/create" class="btn-large waves-effect waves-light purple">
                                <i class="material-icons left">add</i>Agregar Anime
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <% if (animes.length === 0) { %>
            <div class="card-panel center-align">
                <i class="material-icons large grey-text">sentiment_dissatisfied</i>
                <h5>No hay animes en tu colección</h5>
                <p>¡Agrega tu primer anime!</p>
            </div>
        <% } else { %>
            <div class="row">
                <% animes.forEach(anime => { %>
                    <div class="col s12 m6 l4">
                        <div class="card anime-card">
                            <div class="card-image-container">
                                <img src="<%= anime.imagen %>" alt="<%= anime.titulo %>" class="anime-image"
                                     onerror="this.src='https://via.placeholder.com/300x400/6c5ce7/ffffff?text=Error+Imagen'">
                                <div class="card-overlay">
                                    <span class="rating-badge <%= anime.calificacion >= 8.0 ? 'rating-high' : anime.calificacion >= 6.0 ? 'rating-medium' : 'rating-low' %>">
                                        ★ <%= anime.calificacion %>
                                    </span>
                                </div>
                            </div>
                            <div class="card-content">
                                <span class="card-title truncate"><%= anime.titulo %></span>
                                <p><strong>Género:</strong> <%= anime.genero %></p>
                                <p><strong>Episodios:</strong> <%= anime.episodios %></p>
                                <p><strong>Año:</strong> <%= anime.año %></p>
                                <p><strong>Estudio:</strong> <%= anime.estudio %></p>
                            </div>
                            <div class="card-action">
                                <form method="POST" action="/animes/delete/<%= anime.id %>" style="display: inline;">
                                    <button type="submit" class="btn-small red waves-effect waves-light" 
                                            onclick="return confirm('¿Estás seguro de eliminar este anime?')">
                                        <i class="material-icons left">delete</i>Eliminar
                                    </button>
                                </form>
                            </div>
                        </div>
                    </div>
                <% }) %>
            </div>
        <% } %>

        <div class="center-align" style="margin: 40px 0;">
            <a href="/" class="btn waves-effect waves-light grey">
                <i class="material-icons left">home</i>Volver al Inicio
            </a>
        </div>
    </div>

    <!-- Materialize JavaScript -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>
</body>
</html>